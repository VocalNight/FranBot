import { Searchparams } from "../../src/xiavpiclasses/searchparams";

declare module "xiavpi-js" {
    

    export function market(): Market;

    export interface Market {
        get(id: string, params: Params): any;
        search(itemName: string): Searchparams;
    }

    export class Params {
        servers: string[];
        languages: string[];
    }
}